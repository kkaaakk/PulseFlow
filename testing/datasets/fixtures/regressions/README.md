# 回归 Fixture

## 中文说明

第一阶段没有真实跑出可归档的回归 Bug，因此此目录保持为空。只有真实、稳定、可
重放的失败数据才能加入，并且必须同时保存原始 JSONL、Manifest、预期/实际结果和
失败说明；不能为了增加测试数量人为制造回归用例。

第一阶段有意保持此目录为空。

这里只能放由 PulseFlow 真实且可重复的失败产生的数据集。未来每个 Regression Case
都必须包含原始 JSONL 输入、记录预期与实际结果的 Manifest，以及简短的失败说明。
不要为了增加覆盖率而添加虚构的回归用例。
